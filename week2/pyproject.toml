[project]
name = "kyouth-week2"
version = "0.1.0"
requires-python = ">=3.14"
# Add pinned dependencies as a TOML array. Keep empty if none are required.
dependencies = [
	"fastmcp",
	"google-genai",
]