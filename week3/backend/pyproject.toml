[project]
name = "kyouth-week3-backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
	"fastapi",
	"jinja2",
	"python-dotenv",
	"uvicorn[standard]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]


